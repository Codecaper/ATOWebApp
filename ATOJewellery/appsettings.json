{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    //We will use this connection string to actually create the database and create our "category" table inside that database |
    //Following this we need to tell application that we will be using SQL server and it need to use this connection string 
    //to establish the connection with SQL server | To do this we will use the "Entity Framework" And we will create an object of the DB context | using the DB context will be able to use that to make aconnection to the database
     "DefaultConnection": "Server = DESKTOP-A7D94U7\\SQLEXPRESS; Database=Bulky; Encrypt=false; Trusted_Connection=True;" // The name "DefualtConnection" name can be anything we want it be 
  
  }
}
